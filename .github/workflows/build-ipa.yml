name: Show Supported Destinations
on:
  workflow_dispatch

jobs:
  test:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Show supported destinations
        run: xcodebuild -showdestinations -project "iOSProject.xcodeproj" -scheme "iOSProject"
