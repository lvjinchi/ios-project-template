name: List Simulators
on:
  workflow_dispatch

jobs:
  test:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Show all iOS simulators
        run: xcrun xctrace list devices
